package com.helperClass.boat;

import org.openqa.selenium.WebDriver;

import com.pageAction.by;
import com.pageAction.pageAction;
import com.pageObject.loginObject;

//public class LoginHelper extends pageAction {
//	public static void loginUser(WebDriver driver) {
	//	clicker(by.cssSelector, loginObject.loginIcon, driver);
	//	clicker(by.xpath, loginObject.Login, driver);
		//sendKeys(by.xpath, loginObject.email, loginObject.userEmail, driver);
		//sendKeys(by.xpath, loginObject.password, loginObject.userPassword, driver);
	//	clicker(by.xpath, loginObject.submit, driver);
	//}

//}
